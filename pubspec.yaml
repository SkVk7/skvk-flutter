name: skvk_application
description: SKVK Astrology - A comprehensive Vedic astrology application with horoscope, matching, predictions, and audio content features.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  archive: ^3.4.9
  connectivity_plus: ^6.0.0
  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.2
  flutter_riverpod: ^2.4.9
  geolocator: ^12.0.0
  get_it: ^7.6.4
  go_router: ^14.0.0
  google_fonts: ^6.1.0
  http: ^1.1.0
  image_picker: ^1.0.4
  injectable: ^2.4.1
  intl: ^0.19.0
  just_audio: ^0.9.36
  lucide_flutter: ^0.546.0
  palette_generator: ^0.3.3+1
  path: ^1.8.3
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  timezone: ^0.9.2
  universal_html: ^2.2.4
  vector_math: ^2.1.4
  workmanager: ^0.5.2

dev_dependencies:
  build_runner: ^2.4.7
  fake_async: ^1.3.1
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.4.1
  mockito: ^5.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more specific resolution-variant
  # files. For example, the following would include images/a_dot_burr.jpeg
  # and images/a_dot_ham.jpeg if they exist:
  # assets:
  #   - images/

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this format:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
